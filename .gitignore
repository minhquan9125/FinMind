# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Env files with real secrets (see history.md - currently tracked with
# placeholder values only; keep ignoring going forward for new local copies)
.env.local
.env.local.bak
